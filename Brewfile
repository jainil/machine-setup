# ============================================================================
#  Brewfile — declarative package list. Installed via `brew bundle`.
#  Edit freely, then run `./setup.sh homebrew` (or `brew bundle`).
#  Docs: https://github.com/Homebrew/homebrew-bundle
# ============================================================================

# --- Taps -------------------------------------------------------------------

# --- CLI tools (bottles) ----------------------------------------------------
brew "git"
brew "gh"                # GitHub CLI
brew "duti"              # set default apps for file types / URL schemes (used by step 04)
brew "defaultbrowser"    # set the default browser from the CLI
brew "zsh"
brew "wget"
brew "jq"
brew "ripgrep"
brew "fzf"
brew "bat"
brew "eza"
# brew "node"
# brew "python@3.12"

# --- GUI apps (casks) -------------------------------------------------------
cask "helium-browser"
cask "gram"

# cask "raycast"

# --- Fonts (casks) ----------------------------------------------------------
cask "font-jetbrains-mono-nerd-font"
cask "font-fira-code"
cask "font-hack-nerd-font"
